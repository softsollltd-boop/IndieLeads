# -----------------------------------------------------------------------------
# SkyReach Enterprise - Production Environment Template
# Copy this to .env.production and fill in the values
# -----------------------------------------------------------------------------

# --- Server Configuration ---
PORT=3000
NODE_ENV=production
FRONTEND_URL=https://app.yourdomain.com

# --- Database (PostgreSQL) ---
# Recommended: Supabase or Neon.tech
DATABASE_URL="postgresql://user:password@host:port/dbname?sslmode=require"
# Set to 'true' to automatically run prisma migrate deploy on startup
RUN_MIGRATIONS="true"

# --- Cache & Queues (Redis) ---
# Recommended: Upstash.com (Free tier available)
REDIS_URL="rediss://default:password@host:port"

# --- Security ---
# JWT_SECRET: Use a long random string (at least 32 chars)
JWT_SECRET="generate-a-new-random-secret-here"
# MASTER_ENCRYPTION_KEY: MUST be exactly 32 characters for AES-256
MASTER_ENCRYPTION_KEY="32-character-long-encryption-key"

# --- AI & External APIs ---
# Gemini API Key
API_KEY="your-gemini-api-key"

# --- Transactional Email (SMTP/Resend/Postmark) ---
# Needed for system notifications and auth
SMTP_HOST="smtp.resend.com"
SMTP_PORT=587
SMTP_USER="resend"
SMTP_PASS="your-key-here"
MAIL_FROM="IndieLeads <noreply@indieleads.ai>"

# --- Monitoring & Error Tracking ---
SENTRY_DSN=""
VITE_SENTRY_DSN=""

# --- OAuth (Optional) ---
# GOOGLE_CLIENT_ID=""
# GOOGLE_CLIENT_SECRET=""
# MICROSOFT_CLIENT_ID=""
# MICROSOFT_CLIENT_SECRET=""
